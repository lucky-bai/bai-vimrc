# Common aliases
alias lag='ping 4.2.2.1'
alias pg='ping google.com'
alias conda3='source ~/anaconda3/bin/activate'
alias gg='git grep -i'
alias cls='clear'

# Improves wrapping behaviour for certain programs like psql
export PAGER='less -SF'
